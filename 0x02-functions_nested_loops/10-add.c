#include "main.h"
/**
 * add - adds two integers.
 * @d: accepts a single int.
 * @f: accepts a single int.
 *
 * Description: Adds two integers together.
 *
 * Return: value of added integers.
 */
int add(int d, int f)
{
	return (d + f);
}
