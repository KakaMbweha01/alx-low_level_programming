#include <stdio.h>
/**
* main - Entry point
*
* Description: produces a string of words.
* Return: Always 0 (Success)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art, ");
fprintf(stderr, "ERROR:Anything\n");
return (0);
}
