#include "main.h"
/**
 * mul - multiplies two integers
 * @c: first integer
 * @d: second integer
 * Return: answer of multiplied int.
 */
int mul(int c, int d)
{
	int answer = 0;

	while (b > 0)
	{
		answer += a;
		b--;
	}
	return answer;
}
